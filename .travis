language: python
python:
  - "3.8"

services:
  - docker

before_script:
  - docker-compose up --build -d
  - pip install -r requirements.txt

script:
  - python app.py &
  - sleep 3 # Wait for app to start
  - npm test
